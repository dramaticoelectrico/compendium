<!DOCTYPE html>
    html(lang="en", class="no-js")
        head
            include partials/head.pug 
            block head  
        body
            include partials/navigation.pug 
            main
                block main